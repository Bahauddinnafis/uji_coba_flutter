import 'package:flutter/material.dart';
import 'package:uji_coba_flutter/app_view.dart';

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MyAppView();
  }
}